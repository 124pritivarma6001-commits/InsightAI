# Optional: Set this to your deployed FastAPI backend URL (Render, Railway, Fly.io, etc.)
# If left empty, local Vite dev proxy (/api) will be used.
VITE_API_URL=https://your-fastapi-backend.onrender.com
